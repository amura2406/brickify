[project]
name = "lego-mosaic-maker"
version = "0.1.0"
description = "Convert images into LEGO Art mosaics"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.30.0",
    "pillow>=11.0.0",
    "numpy>=2.0.0",
    "python-multipart>=0.0.9",
]

[tool.uv]
dev-dependencies = []
